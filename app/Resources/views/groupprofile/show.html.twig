{% extends 'base.html.twig' %}

{% block body %}
    <h1>Groupprofile</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ groupProfile.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('group-profile_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('group-profile_edit', { 'id': groupProfile.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
